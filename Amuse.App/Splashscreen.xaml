<Window x:Class="Amuse.App.Splashscreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="NoResize"
        Height="350"
        Width="600"
        Background="Transparent" 
        Foreground="#FFC8C8C8"
        WindowStartupLocation="CenterScreen" 
        Icon="/Images/Icon.ico" 
        Cursor="AppStarting" 
        Focusable="False"
        IsHitTestVisible="False"
        IsTabStop="False"
        Name="UI">
    <Border DataContext="{Binding ElementName=UI}" UseLayoutRounding="True" Background="#FF272727" BorderBrush="#80d3ae58" BorderThickness="1" SnapsToDevicePixels="True" RenderOptions.BitmapScalingMode="Fant">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Image Source="/Images/Splash_Logo.png" Width="200" Opacity=".95" />
            <TextBlock Text="{Binding Title}" TextAlignment="Center" FontSize="15"/>
        </StackPanel>
    </Border>
</Window>
