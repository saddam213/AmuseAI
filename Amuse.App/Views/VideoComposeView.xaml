<Views:ViewBase x:Class="Amuse.App.Views.VideoComposeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:Amuse.App.Views"
    xmlns:CommonBehaviors="clr-namespace:TensorStack.WPF.Behaviors;assembly=TensorStack.WPF"
    xmlns:CommonControls="clr-namespace:TensorStack.WPF.Controls;assembly=TensorStack.WPF"
    xmlns:Views="clr-namespace:Amuse.App.Views"
    xmlns:Controls="clr-namespace:Amuse.App.Controls"
    xmlns:Base="clr-namespace:Amuse.App">
    <Grid DataContext="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:VideoComposeView}}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition MinWidth="380"/>
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="4*" />
            </Grid.ColumnDefinitions>


            <!--***Left Panel***-->
            <Grid Grid.Column="0" >
                <Border Margin="4">
                    <DockPanel x:Name="LeftPanelContainer" Margin="4">

                        <!--Execute/Cancel-->
                        <UniformGrid DockPanel.Dock="Bottom" Columns="2" Height="30">
                            <!--<Button Content="Clear" Command="{Binding ClearCommand, ElementName=LayerControl}" />
                            <Button Content="Save" Command="{Binding SaveCommand, ElementName=LayerControl}" />-->
                        </UniformGrid>

                        <DockPanel>



                        </DockPanel>
                    </DockPanel>
                </Border>
            </Grid>


            <!--GridSplitter-->
            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Width="5" Opacity=".4" Margin="0,20" />


            <!--***Right Panel***-->
            <Grid Grid.Column="2" Grid.Row="0" >

                <!--Content-->
                <DockPanel>
                    <Border Margin="4">

                        <UniformGrid Columns="2" Margin="10">

                            <Border Margin="10,10,10,2">
                                <CommonControls:ImageElement 
                                    SplitterVisibility="Auto"
                                    SplitterPosition="Source"
                                    SplitterDirection="LeftToRight"
                                    IsLoadEnabled="True" 
                                    IsSaveEnabled="False"
                                    IsLoadOverlayEnabled="True"/>
                            </Border>


                            <Border Margin="10,10,10,2"  >


                                <CommonControls:VideoStreamElement 
                                        x:Name="ResultControl" 
                                        SplitterVisibility="Auto"
                                        IsInputEnabled="True"
                                        SplitterPosition="Source"
                                        SplitterDirection="LeftToRight"
                                        IsLoadEnabled="True" 
                                        IsLoadOverlayEnabled="True"
                                        IsFrameViewerEnabled="True"
               
                                        Volume="{Binding Settings.VolumeOutput, Mode=TwoWay}"
                                        IsVolumeMute="{Binding Settings.IsVolumeOutputMute, Mode=TwoWay}"/>
                            </Border>
                        </UniformGrid>

                    </Border>
                </DockPanel>

            </Grid>
        </Grid>

    </Grid>
</Views:ViewBase>
